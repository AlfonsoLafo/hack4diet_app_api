$kind: http-request
name: change password
method: PUT
url: "{{baseUrl}}/api/usuarios/change-password/{{userId}}"
order: 6000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "currentPassword": "demo",
      "newPassword": "newpassword123"
    }
